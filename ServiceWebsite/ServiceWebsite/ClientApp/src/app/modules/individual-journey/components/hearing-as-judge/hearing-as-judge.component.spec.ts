import { HearingAsJudgeComponent } from './hearing-as-judge.component';
import { CanCreateComponent } from '../individual-base-component/component-test-bed.spec';

describe('HearingAsJudgeComponent', () => {
  it('can be created', () => {
    CanCreateComponent(HearingAsJudgeComponent);
  });
});
