import { VideoWebService } from './video-web.service';
import { ApiClient } from '../../../services/clients/api-client';

describe('VideoWebService', () => {

  let service: VideoWebService;

  service = new VideoWebService(new MockLogger());
});
