import { QuestionnaireCompletedComponent } from './questionnaire-completed.component';
import { CanCreateComponent } from '../representative-base-component/component-test-bed.spec';

describe('QuestionnaireCompletedComponent', () => {
  it('can be created', () => {
    CanCreateComponent(QuestionnaireCompletedComponent);
  });
});
